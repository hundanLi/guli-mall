spring:
  cloud:
    nacos:
      # 配置中心
      config:
        server-addr: 127.0.0.1:8848
        namespace: gulimall-gateway
        group: dev
      # 注册中心
      discovery:
        server-addr: 127.0.0.1:8848
  # 应用名称
  application:
    name: gulimall-gateway